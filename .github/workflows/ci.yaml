name: Compile Test

on: [push, pull_request]

jobs:
  compile_job:
    name: Compile + Parser test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up JDK
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: '21'

      - name: Compile
        run: javac -d out $(find src test -name '*.java')

      - name: Run Parser Tests
        run: java -cp out server.ParserTest

      - name: Run Waiter Test
        run: java -cp out server.WaiterTest

      - name: Run BLPOP Test
        run: java -cp out server.BlpopTest

      - name: Run Concurrency Test
        run: java -cp out server.ConcurrencyTest
